---
"svedocs": patch
---

Add an opt-in bounded per-isolate HTML cache for public SSR pages, preserving agent negotiation and conditional requests. Pin server virtual configuration to the effective build mode so CLI static/SPA overrides also reach runtime integrations.
