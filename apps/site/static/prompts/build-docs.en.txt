Build a complete, runnable documentation site for the project in the current workspace using svedocs, with a fully custom theme and landing page that fit this project's identity.

1. Install and read the official Agent Skills from https://github.com/backrunner/svedocs/tree/main/skills: use-svedocs, configure-svedocs, customize-svedocs-theme, and build-svedocs-landing. Also use localize-svedocs if the project needs multiple languages. Use your agent's skill installer to install these folders from backrunner/svedocs, or copy the complete folders, including references, into this project's .agents/skills directory. Preserve existing skills. Read each relevant SKILL.md and its references before implementing. Official documentation: https://svedocs.pwp.sh/llms.txt. Confirm that the APIs you use match the installed package version.

2. Inspect the repository instructions, README, package manifests, source code, existing docs, and UI. Identify the audience, main workflows, real installation commands, public APIs, and visual identity: logo, colors, typography, spacing, imagery, and interaction patterns. Use this evidence to choose the documentation structure and design direction. If no visual identity exists, create a coherent one suited to the product and briefly explain it. Make reasonable decisions and continue; ask only when essential information cannot be inferred.

3. Respect the existing workspace and package manager. Integrate with an existing SvelteKit documentation app where appropriate; otherwise create a dedicated docs app in a suitable empty directory using the official docs template (for pnpm: pnpm create svedocs <directory> --template docs). The generator also includes .agents/skills. Preserve existing routes, content, and unrelated work. Install dependencies and keep svedocs configuration in svedocs.config.ts.

4. Implement a complete custom theme and landing page. Design typography, colors, light and dark modes, header, sidebar, article layout, table of contents, code blocks, search, and footer as one system. Build a project-specific hero, useful product visuals or working examples, evidence-based feature sections, and clear links to the first successful workflow. Use the project's real branding and assets. Go beyond recoloring the starter: use Svelte components, Tailwind CSS v4, theme tokens, slots, or component overrides while preserving svedocs routing, metadata, localization, search, and accessibility. Make the design work on mobile and desktop, with keyboard navigation, visible focus, sufficient contrast, and reduced-motion support.

5. Write useful documentation from the actual project: overview, quick start, installation, configuration, main workflows, relevant API or CLI reference, deployment, and troubleshooting. Choose sections that apply to this project. Verify commands and examples against the source; do not invent features, APIs, benchmarks, testimonials, or customer logos. Mark any missing facts clearly. Replace template filler and keep navigation, internal links, and examples consistent.

6. Configure useful page titles and descriptions, canonical URLs using the real public site URL when known, sitemap, robots, and branded 1200×630 PNG Open Graph images with meaningful alt text. Preserve markdown twins and llms.txt. Use local search initially; enable hosted search or Ask AI only when the required runtime and configuration exist. Choose edge or static output to match the hosting target. If the public domain is unknown, make it an explicit remaining configuration item rather than inventing one.

7. Complete the implementation and verify it: run the project's type checks, pnpm exec svedocs check --strict (or the package-manager equivalent), and the production build. Test the selected deployment mode. Use a browser to check the homepage, a documentation page, search, navigation, code copying, mobile layout, both theme modes, broken links, and social metadata/image URLs. Fix failures. Deliver the working code, a brief explanation of the design, local preview commands, validation results, and any remaining configuration. Do not stop at a plan or mockup.
